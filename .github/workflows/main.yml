name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: run tests
        run: dotnet test -v n
      - name: create the nuget package
        run: dotnet pack -c Release